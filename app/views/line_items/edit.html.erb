<%# app/views/line_items/edit.html.erb %>

<main class="container">
    <%= link_to sanitize("&larr; Retour aux devis"), quote_path(@quote) %>

    <div class="header">
        <h1>Modifier ligne</h1>
    </div>

    <%= turbo_frame_tag @line_item do %>
        <%= render "form",
              quote: @quote,
              line_item_date: @line_item_date,
              line_item: @line_item %>
    <% end %>
</main>
